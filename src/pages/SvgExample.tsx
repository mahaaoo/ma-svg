import React from 'react';
import {View} from 'react-native';

interface SvgExampleProps {
};

const SvgExample: React.FC<SvgExampleProps> = props => {
  const {} = props;

  return (
    <View />
  )
};

export default SvgExample;
